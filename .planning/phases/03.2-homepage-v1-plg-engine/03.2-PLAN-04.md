---
phase: 03.2-homepage-v1-plg-engine
plan: 04
type: standard
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/components/homepage/HowItWorks.tsx
  - src/components/homepage/ManifestoBand.tsx
  - src/components/homepage/Footer.tsx
  - src/app/page.tsx
requirements_addressed: [HOME-06, HOME-07]
autonomous: true
---

# Plan 04: Act 5 (The Closer) & Footer — Final Assembly

<objective>
Build the 3-column "Manifesto as How-It-Works" section, the full-width forest green
Manifesto Band with entrance animation and dual glassmorphism CTAs, and the hyper-minimal
footer. Replace ALL remaining old homepage sections. Final page.tsx should contain ONLY
the 5 Acts + Footer from the WIREFRAME.md.
</objective>

<context>
@_bmad-output/brainstorming/WIREFRAME.md (Act 5, Footer)
@src/app/page.tsx (remaining old sections to remove)
@src/components/ScrollReveal.tsx (existing scroll animation component)
</context>

<tasks>

<task>
<description>Create HowItWorks 3-column component</description>
<read_first>
- _bmad-output/brainstorming/WIREFRAME.md (Act 5 Part A)
- src/app/page.tsx (PROCESS_STEPS for reference — will be replaced)
</read_first>
<action>
Create `src/components/homepage/HowItWorks.tsx` as a **server component**:

1. **Data**:
   ```typescript
   const COLUMNS = [
     {
       title: 'Выберите мастера',
       description: 'Подтверждённые мастера, реальные фотографии работ и прозрачные цены.',
     },
     {
       title: 'Запишитесь за 30 секунд',
       description: 'Без переписок. Без ожидания. Мгновенное подтверждение. Ваше время важно.',
     },
     {
       title: 'Качество по нашим стандартам',
       description: 'Каждый мастер проверен. Каждый отзыв реален. Без компромиссов.',
     },
   ];
   ```

2. **Section**: `<section className="py-12 md:py-16 bg-booking-bg">`
   - Inner: `<div className="max-w-7xl mx-auto px-4 md:px-8">`

3. **Grid**: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">`

4. **Each column card**:
   ```tsx
   <div className="bg-booking-card rounded-[2rem] shadow-soft-out p-8 text-center">
     <h3 className="font-serif text-xl md:text-2xl font-bold text-booking-textMain">
       {col.title}
     </h3>
     <p className="font-sans text-sm md:text-base text-booking-textMuted mt-4 leading-relaxed">
       {col.description}
     </p>
   </div>
   ```
</action>
<acceptance_criteria>
- File `src/components/homepage/HowItWorks.tsx` exists
- Contains 3 column items with exact Russian copy from wireframe
- Uses `shadow-soft-out` and `bg-booking-card` on each card
- `font-serif` on titles, `font-sans` on descriptions
- Responsive: single column on mobile, 3 columns on md+
</acceptance_criteria>
</task>

<task>
<description>Create ManifestoBand component with entrance animation and dual CTAs</description>
<read_first>
- _bmad-output/brainstorming/WIREFRAME.md (Act 5 Part B)
- src/components/ScrollReveal.tsx (existing animation approach — we'll use our own for manifesto)
</read_first>
<action>
Create `src/components/homepage/ManifestoBand.tsx` as a **client component** ('use client' for Intersection Observer entrance animation):

1. **Intersection Observer**: Detect when band enters viewport. Set `isVisible` to trigger `animate-manifesto-enter` class on text and CTAs.

2. **Section**: `<section className="bg-booking-primary py-16 md:py-20">`
   - Inner: `<div className="max-w-4xl mx-auto px-4 md:px-8 text-center">`

3. **Manifesto line** (animated):
   ```tsx
   <h2
     className={`font-serif text-2xl md:text-4xl font-bold text-white transition-all duration-600 ${
       isVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-5'
     }`}
   >
     Мы не идём на компромиссы. И вы не должны.
   </h2>
   ```

4. **Dual CTAs** (animated with 300ms delay):
   ```tsx
   <div
     className={`mt-8 flex flex-col sm:flex-row justify-center gap-4 transition-all duration-600 delay-300 ${
       isVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-5'
     }`}
   >
     <Link
       href="/search"
       className="rounded-full bg-white/15 backdrop-blur-sm border border-white/30 text-white px-8 py-4 text-sm font-bold hover:bg-white/25 transition-colors"
     >
       Найти мастера
     </Link>
     <Link
       href="/become-pro"
       className="rounded-full bg-white text-booking-primary px-8 py-4 text-sm font-bold hover:bg-white/90 transition-colors"
     >
       Стать мастером
     </Link>
   </div>
   ```

   **Design note**: "Стать мастером" button is solid white (higher visual weight) — this is the PLG conversion target.
</action>
<acceptance_criteria>
- File `src/components/homepage/ManifestoBand.tsx` exists
- Contains 'use client' directive
- Contains `IntersectionObserver` in useEffect
- Background is `bg-booking-primary` (forest green #2F4B3A)
- Contains exact text "Мы не идём на компромиссы. И вы не должны."
- Contains two CTAs: "Найти мастера" linking to `/search` and "Стать мастером" linking to `/become-pro`
- "Стать мастером" uses `bg-white text-booking-primary` (solid white, higher weight)
- Text fades in with translateY animation on scroll
- CTAs animate 300ms after text
</acceptance_criteria>
</task>

<task>
<description>Create hyper-minimal Footer component</description>
<read_first>
- _bmad-output/brainstorming/WIREFRAME.md (Footer spec)
</read_first>
<action>
Create `src/components/homepage/Footer.tsx` as a **server component**:

1. **Container**: `<footer className="bg-slate-950 py-8">`
   - Inner: `<div className="max-w-7xl mx-auto px-4 md:px-8 text-center">`

2. **Links row**: `<div className="flex justify-center gap-6 text-xs text-white/50">`
   - `<Link href="/legal">Правовая информация</Link>`
   - `<Link href="/privacy">Политика конфиденциальности</Link>`
   - `<a href="https://instagram.com/svoi.de" target="_blank" rel="noopener noreferrer">Instagram</a>`

3. **Sign-off line**: `<p className="font-serif italic text-xs md:text-sm text-white/30 mt-4">Designed in Europe. Built for our standards.</p>`

Hyper-minimal. No sitemap. No mega-footer. The sign-off bookends the Hero's cultural promise.
</action>
<acceptance_criteria>
- File `src/components/homepage/Footer.tsx` exists
- Contains links to `/legal`, `/privacy`, and Instagram
- Contains text "Designed in Europe. Built for our standards."
- Sign-off uses `font-serif italic`
- Background is `bg-slate-950`
- Total lines of code < 30
</acceptance_criteria>
</task>

<task>
<description>Final page.tsx assembly — remove ALL old sections, wire final components</description>
<read_first>
- src/app/page.tsx
- src/components/homepage/HowItWorks.tsx
- src/components/homepage/ManifestoBand.tsx
- src/components/homepage/Footer.tsx
</read_first>
<action>
Rewrite `src/app/page.tsx` to contain ONLY the 5 Acts + Footer:

```typescript
import { Suspense } from 'react';
import HomeHeroV2 from '@/components/homepage/HomeHeroV2';
import CategoryNav from '@/components/homepage/CategoryNav';
import MasterGallery from '@/components/homepage/MasterGallery';
import HowItWorks from '@/components/homepage/HowItWorks';
import ManifestoBand from '@/components/homepage/ManifestoBand';
import HomepageFooter from '@/components/homepage/Footer';

export const dynamic = 'force-dynamic';

export default function HomePage() {
  return (
    <div className="min-h-screen bg-booking-bg">
      {/* Act 1: The Gateway */}
      <HomeHeroV2 />

      {/* Act 2: The Appetite */}
      <CategoryNav />

      {/* Act 3 + Act 4: The Proof + PLG Trojan Horse */}
      <Suspense fallback={<div className="h-96 w-full animate-pulse bg-booking-bg" />}>
        <MasterGallery />
      </Suspense>

      {/* Act 5a: The Reassurance */}
      <HowItWorks />

      {/* Act 5b: The Closer */}
      <ManifestoBand />

      {/* The Sign-Off */}
      <HomepageFooter />
    </div>
  );
}
```

**Delete ALL old constants**: TRUST_METRICS, FEATURE_COLUMNS, DISCOVERY_CARDS, PROCESS_STEPS, BENEFITS, CITY_PILLS.
**Remove ALL old section JSX** and unused imports (ArrowRight, CalendarCheck, Clock3, MapPin, Search, ShieldCheck, Sparkles, Star, TrendingUp, UserCheck, ScrollReveal, TopMastersSection, nextDynamic, Link).
</action>
<acceptance_criteria>
- `src/app/page.tsx` contains exactly 6 component imports (HomeHeroV2, CategoryNav, MasterGallery, HowItWorks, ManifestoBand, HomepageFooter) plus Suspense
- No TRUST_METRICS, FEATURE_COLUMNS, DISCOVERY_CARDS, PROCESS_STEPS, BENEFITS, or CITY_PILLS arrays
- No lucide-react icon imports
- No `<ScrollReveal>` usage in page.tsx
- File is under 35 lines of code
- `npm run build` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm run build 2>&1 | tail -10

# Full page structure check:
curl -s http://localhost:3000 | grep -o "Свои мастера" | head -1
curl -s http://localhost:3000 | grep -o "Маникюр" | head -1
curl -s http://localhost:3000 | grep -o "Наши мастера" | head -1
curl -s http://localhost:3000 | grep -o "Хотите такой же профиль" | head -1
curl -s http://localhost:3000 | grep -o "Выберите мастера" | head -1
curl -s http://localhost:3000 | grep -o "Мы не идём на компромиссы" | head -1
curl -s http://localhost:3000 | grep -o "Built for our standards" | head -1
```
All 7 greps must return a match — confirming all 5 Acts + Footer are rendered.
</verification>

<success_criteria>
- Complete homepage scroll story: Hero → Categories → Gallery+JealousyCard → HowItWorks → Manifesto → Footer
- Zero old homepage sections remain
- page.tsx is clean and minimal (~30 lines)
- Manifesto band entrance animation works on scroll
- Dual CTAs render with correct visual weight hierarchy
- Footer bookends the Hero's cultural promise
- Build passes with zero errors
</success_criteria>

<must_haves>
- 3-column neumorphic HowItWorks
- Full-width forest green ManifestoBand with entrance animation
- Dual CTAs: ghost "Найти мастера" + solid "Стать мастером"
- Hyper-minimal footer with sign-off
- Clean page.tsx with only component composition
</must_haves>
