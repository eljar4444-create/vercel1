---
phase: 03.2-homepage-v1-plg-engine
plan: 02
type: standard
wave: 2
depends_on: [01]
files_modified:
  - src/components/homepage/HomeHeroV2.tsx
  - src/components/homepage/CategoryNav.tsx
  - src/app/page.tsx
requirements_addressed: [HOME-02, HOME-03]
autonomous: true
---

# Plan 02: Act 1 (Hero) & Act 2 (Category Navigation)

<objective>
Build the new Hero section with cultural H1 ("Свои мастера. Тот самый уровень качества."),
SEO German subtitle, and neumorphic city pill buttons. Build the horizontal-scroll
category navigation with macro-photography placeholders. Wire both into page.tsx
replacing the old HomeHero and trust metrics section.
</objective>

<context>
@_bmad-output/brainstorming/WIREFRAME.md (Act 1, Act 2)
@src/components/HomeHero.tsx (current hero — will be replaced)
@src/app/page.tsx (current homepage — top sections will be replaced)
@tailwind.config.ts (booking tokens, shadow-soft-out/in)
</context>

<tasks>

<task>
<description>Create HomeHeroV2 component</description>
<read_first>
- src/components/HomeHero.tsx
- src/app/page.tsx
- _bmad-output/brainstorming/WIREFRAME.md
</read_first>
<action>
Create `src/components/homepage/HomeHeroV2.tsx` as a **server component** (no 'use client'):

1. **Container**: `section` with `min-h-[60vh] md:min-h-[70vh] bg-booking-bg flex flex-col items-center justify-center px-4 md:px-8`

2. **H1**: Two lines using `font-serif`:
   - Line 1: "Свои мастера."
   - Line 2: "Тот самый уровень качества."
   - Classes: `text-4xl md:text-6xl font-extrabold text-booking-textMain tracking-tight text-center`
   - Use `<br />` or two `<span className="block">` for line break

3. **SEO Subtitle**: `<p>` with text "Finde und buche die besten Beauty-Meister in Deutschland"
   - Classes: `font-sans text-base md:text-lg text-booking-textMuted text-center mt-4`

4. **City Pills**: Array `['Berlin', 'München', 'Hamburg', 'Frankfurt', 'Köln', 'Düsseldorf']`
   - Container: `mt-8 flex flex-wrap justify-center gap-3`
   - Each pill: `<Link href="/search?city={city}">` with classes:
     `rounded-full px-5 py-2.5 bg-booking-card btn-neu text-sm font-semibold text-booking-textMain`
   - No master counts on pills.

No search bar. No video. No stats. Pure elegance.
</action>
<acceptance_criteria>
- File `src/components/homepage/HomeHeroV2.tsx` exists
- Contains H1 text "Свои мастера." and "Тот самый уровень качества."
- Contains German subtitle with "Finde und buche"
- Contains 6 city pill `<Link>` elements linking to `/search?city=Berlin` etc.
- Pills use `btn-neu` class for neumorphic press effect
- Component is a server component (no 'use client' directive)
</acceptance_criteria>
</task>

<task>
<description>Create CategoryNav component</description>
<read_first>
- src/app/page.tsx (DISCOVERY_CARDS array for current categories)
- _bmad-output/brainstorming/WIREFRAME.md (Act 2 spec)
</read_first>
<action>
Create `src/components/homepage/CategoryNav.tsx` as a **client component** ('use client' for scroll snap):

1. **Category data**: Array of objects with `name`, `href`, `imageSrc` (placeholder path for now):
   ```typescript
   const CATEGORIES = [
     { name: 'Маникюр', href: '/search?q=Маникюр', imageSrc: '/categories/manicure.webp' },
     { name: 'Педикюр', href: '/search?q=Педикюр', imageSrc: '/categories/pedicure.webp' },
     { name: 'Брови и ресницы', href: '/search?q=Брови', imageSrc: '/categories/brows.webp' },
     { name: 'Волосы', href: '/search?q=Стрижка', imageSrc: '/categories/hair.webp' },
     { name: 'Массаж', href: '/search?q=Массаж', imageSrc: '/categories/massage.webp' },
     { name: 'Косметология', href: '/search?q=Косметология', imageSrc: '/categories/cosmetology.webp' },
   ];
   ```

2. **Container**: `section` with `py-8 md:py-12 bg-booking-bg`
   - Inner: `max-w-7xl mx-auto px-4 md:px-8`
   - Desktop: `md:flex md:justify-center md:gap-6`
   - Mobile: `flex overflow-x-auto gap-4 snap-x snap-mandatory scrollbar-hide`

3. **Category card** (each item):
   - `<Link>` wrapper with classes: `snap-center flex-shrink-0 w-28 h-36 md:w-36 md:h-44 rounded-[2rem] bg-booking-card btn-neu flex flex-col items-center justify-center gap-3`
   - Image: `<Image>` with `w-16 h-16 md:w-20 md:h-20 rounded-2xl object-cover` — use Next.js Image with fallback div if image missing
   - Label: `<span className="font-sans text-xs md:text-sm font-semibold text-booking-textMain">`

4. **scrollbar-hide**: Add to globals.css if not present:
   ```css
   .scrollbar-hide::-webkit-scrollbar { display: none; }
   .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
   ```
</action>
<acceptance_criteria>
- File `src/components/homepage/CategoryNav.tsx` exists
- Contains 'use client' directive
- Contains 6 category items (Маникюр through Косметология)
- Each links to `/search?q={service}`
- Uses `snap-x snap-mandatory` for mobile scroll snap
- Uses `btn-neu` class on each card
- Mobile: `overflow-x-auto`, Desktop: `md:flex md:justify-center`
</acceptance_criteria>
</task>

<task>
<description>Wire Hero and CategoryNav into page.tsx, remove old Hero and trust metrics</description>
<read_first>
- src/app/page.tsx
- src/components/homepage/HomeHeroV2.tsx
- src/components/homepage/CategoryNav.tsx
</read_first>
<action>
Edit `src/app/page.tsx`:

1. **Remove imports**: `HomeHero`, `TRUST_METRICS` section
2. **Add imports**: `import HomeHeroV2 from '@/components/homepage/HomeHeroV2'` and `import CategoryNav from '@/components/homepage/CategoryNav'`
3. **Replace** the `<HomeHero />` component with `<HomeHeroV2 />`
4. **Replace** the TRUST_METRICS `<section>` with `<CategoryNav />`
5. Keep ALL other sections below (FEATURE_COLUMNS, DISCOVERY_CARDS, TopMastersSection, PROCESS_STEPS, BENEFITS, final CTA) temporarily — they will be replaced in later plans.
6. Set page background: Add `bg-booking-bg` to the root `<div className="min-h-screen">` → `<div className="min-h-screen bg-booking-bg">`
</action>
<acceptance_criteria>
- `src/app/page.tsx` imports `HomeHeroV2` from `@/components/homepage/HomeHeroV2`
- `src/app/page.tsx` imports `CategoryNav` from `@/components/homepage/CategoryNav`
- No import of `HomeHero` from `@/components/HomeHero`
- Root div has `bg-booking-bg` class
- `npm run build` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
npm run build 2>&1 | tail -10
# Verify homepage renders:
curl -s http://localhost:3000 | grep -o "Свои мастера" | head -1
curl -s http://localhost:3000 | grep -o "Маникюр" | head -1
```
</verification>

<success_criteria>
- Homepage renders new Hero with cultural H1
- City pills visible and linking to /search?city=X
- Category nav scrollable on mobile, centered on desktop
- Old hero with search bar, video, and stats removed
- Build passes
</success_criteria>

<must_haves>
- Cultural H1 in Playfair Display
- German SEO subtitle
- Neumorphic city pills with btn-neu press
- Horizontal scroll category navigation
</must_haves>
